import React from 'react';
import {Alert} from "react-bootstrap";

const NotFound = () => {
    return (
        <div>
            <Alert variant={"danger"}>Not Found Pages</Alert>
        </div>
    );
};

export default NotFound;